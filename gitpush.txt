
git checkout dev && git add . && git commit -m "update" && git push origin dev

    git checkout dev
    git add .
    git commit -m "Yeni özellik eklendi"
    git push origin dev


git checkout main && git merge dev && git push origin main

    git checkout main
    git merge dev
    git push origin main
